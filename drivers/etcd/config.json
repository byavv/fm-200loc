{
    "config": {
        "connection_string": {
            "default": "localhost:4001",
            "required": true,
            "label": "Connection string",
            "help": "Your etcd server host"
        },
        "heartbeat_interval": {
            "default": "15",
            "required": true,
            "label": "Heartbeat interval",
            "help": "Etcd storage key update time (s)"
        }
    }
}