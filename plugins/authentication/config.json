{
    "config": {
        "grant": {
            "default": "*",
            "required": true,
            "label": "Grant (read, write, etc..)",
            "help": "Grant access for roles, which has this permissions"
        }
    },
    "require": []
}