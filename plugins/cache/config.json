{
    "config": {
        "forCaching": {
            "default": "get:/",
            "required": true,
            "label": "Cache path",
            "help": "Cached data for request"
        }
    }
}