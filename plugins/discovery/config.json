{
    "config": {
        "mapTo": {
            "default": "",
            "required": true,
            "label": "Target",
            "help": "Your etcd server key to discover ip address"
        }
    },
    "$require": [
        "etcd"
    ]
}