{
    "config": {
        "target": {
            "default": "localhost:80",
            "required": true,
            "label": "Target",
            "help": "Proxy target"
        },
        "withPath": {
            "default": "/",
            "required": true,
            "label": "Path",
            "help": "Path to be added to target api url"
        }
    },
    "$require": [
        "proxy"
    ]
}